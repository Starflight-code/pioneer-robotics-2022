class spin {
private:
    int spin_color;
    int correct_color =
        1; // UNKNOWN -> Add correct color between red: 1 or blue: 2
    int get_color() {
        // Include color sensor code, to convert values to red: 1 or blue: 2
        return 1;
    };
    void rotate(){
        // Inlude code to rotate the motor and stop on the correct color
    };
};